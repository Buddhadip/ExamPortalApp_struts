<%@ taglib prefix="c" uri="http://java.sun.com/jsp/jstl/core" %>
<c:forEach var="notification" items="${notification}">
<div class="notification"> 
        <div class="text">
          <div>  <span class="bold">${result.getUname() } </span> submmited 
          <span class="bold">${result.getEname()}</span>  at 
          <span class="bold">${result.getSubmission()}</span> </div>
          <div>
            <span class="bold"> Score:</span> ${result.getEname()}
            <span class="bold"> Total:</span> ${result.getTotal()}
          </div>
        </div>
        <div class="cross">
            <button>x</button>
        </div>
</div>
 </c:forEach>